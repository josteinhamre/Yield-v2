import { setEventListener } from '../components/profile_tabs';
import { setupProfileIcons } from '../components/icons';

document.addEventListener("DOMContentLoaded", () => {
  setEventListener();
})
setupProfileIcons();
