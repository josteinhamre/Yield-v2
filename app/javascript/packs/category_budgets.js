import { setColors, setColor } from '../components/progress_color';
import { submitOn, submitOnThis } from '../components/submit_on';
import { replaceBudget, replaceCategories, replaceInfo, replaceMonth } from '../components/replace_elements';



window.setColors = setColors;
window.setColor = setColor;
window.submitOn = submitOn;
window.submitOnThis = submitOnThis;
window.replaceBudget = replaceBudget;
window.replaceCategories = replaceCategories;
window.replaceInfo = replaceInfo;
window.replaceMonth = replaceMonth;


setColors();
submitOn();

