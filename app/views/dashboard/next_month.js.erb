document.getElementById("monthSelector").innerHTML = "<%= j @selected_month %>";

(() => {
  let income = "<%= current_user.income_for_month(@selected_month).sum(:amount_cents) / 100 %>";
  let spent = "<%= @transactions_no_income.sum(:amount_cents).abs / 100 %>";
  let budgeted = "<%= current_user.budgets_for_month(@selected_month).sum(:amount_cents) / 100 %>";
  let balance = "<%= current_user.balance_for_month(@selected_month).last %>";



  // set values for fields on page
  document.getElementById('income-value').innerText = income;
  document.getElementById('spent-value').innerText = spent;
  document.getElementById('budgeted-value').innerText = budgeted;
  document.getElementById('balance-value').innerText = balance;
  // document.querySelector('[data-id=spent] .info-card-graph-header h5:nth-of-type(2)')
  //   .innerText = spent;
  // document.querySelector('[data-id=budgeted] .info-card-graph-header h5:nth-of-type(2)')
  //   .innerText = budgeted;
  // document.querySelector('[data-id=balance] .info-card-graph-header h5:nth-of-type(2)')
  //   .innerText = balance;
})();

loadCharts();
