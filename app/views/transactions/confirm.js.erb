function confirmTrans() {
  document.getElementById("transaction-<%= @transaction.id %>").classList.add('confirmed')
  document.getElementById("row-selector-<%= @transaction.id %>").classList.remove('display')
  document.getElementById("row-selector-<%= @transaction.id %>").classList.add('hidden')

  if (document.querySelectorAll('.confirmed').length === document.querySelectorAll('.category-selector-div').length) {
    document.getElementById('empty-inbox').classList.remove('hidden')
    document.getElementById('inboxListHeader').classList.add('hidden')
  }


// let nextElementId = document.getElementById("chevron-<%= @transaction.id %>")
//   .parentElement.querySelector(".transaction-arrow")
//   .dataset.nextElementId

// console.log(document.getElementById(nextElementId).parentElement.querySelector('.trans-cat'))
// document.getElementById(nextElementId).parentElement.querySelector('.trans-cat').click()


}


confirmTrans();
