      <h4>Upload a transactionlist:</h4>
<%= simple_form_for :transactions, url: transactions_path do |f| %>
  <div class="upload-btn-wrapper">
    <button class="btn-upload">Chose file!</button>
    <%= f.input_field :file, as: :file, id: "upload" %>
  </div>
  <%= f.submit 'upload', type: 'hidden' %>
<% end %>
      <h6>We currently support csv as .txt or .csv from DNB</h6>
