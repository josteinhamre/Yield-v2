<% current_user.categories.each do |category| %>
      <%= render 'components/category_listitem', category: category, index: category.id, budgets: budgets, selected_month: selected_month %>
<% end %>
